# ระบบคำขอใบกำกับภาษี (Tax Invoice Request System)

> **เอกสารออกแบบระบบ** — ChatCenterAI  
> **วันที่สร้าง:** 2026-02-14  
> **สถานะ:** Draft

---

## 1. ภาพรวม (Overview)

ระบบคำขอใบกำกับภาษีเป็นฟีเจอร์เสริมที่ให้ AI chatbot สามารถสร้างลิงก์ฟอร์มเพื่อให้ลูกค้ากรอกข้อมูลสำหรับออกใบกำกับภาษี โดยลิงก์ดังกล่าวจะผูกกับออเดอร์ที่ระบุและลูกค้าคนนั้น ๆ โดยอัตโนมัติ

### 1.1 เป้าหมาย

- ให้ลูกค้าสามารถขอใบกำกับภาษีผ่านแชทได้โดยตรง
- ลดภาระแอดมินในการสอบถามข้อมูลสำหรับออกใบกำกับภาษี
- เก็บข้อมูลใบกำกับภาษีอย่างเป็นระบบ พร้อมลิงก์กับออเดอร์

### 1.2 ขอบเขต

- **ในขอบเขต:** AI tool, ฟอร์มกรอกข้อมูล, หน้า admin จัดการ, API
- **นอกขอบเขต:** การออกใบกำกับภาษีจริง (PDF) — แอดมินจัดการเอง

---

## 2. Flow การทำงาน

```
┌─────────────────────────────────────────────────────────────────┐
│                        CUSTOMER FLOW                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ลูกค้า: "ขอใบกำกับภาษีครับ"                                      │
│       │                                                         │
│       ▼                                                         │
│  AI เรียก get_orders() → ได้ orderId                              │
│       │                                                         │
│       ▼                                                         │
│  AI เรียก request_tax_invoice({ orderId: "xxx" })                 │
│       │                                                         │
│       ▼                                                         │
│  ระบบ:                                                          │
│    1. ตรวจสอบ orderId + userId                                   │
│    2. ตรวจสอบซ้ำ (ถ้ามีอยู่แล้ว ส่ง link เดิม)                      │
│    3. สร้าง record ใน tax_invoices (status: pending)               │
│    4. สร้าง unique token                                         │
│    5. Return { success, formUrl }                                │
│       │                                                         │
│       ▼                                                         │
│  AI ส่งลิงก์ให้ลูกค้า:                                            │
│  "กรุณากรอกข้อมูลที่ลิงก์นี้: https://xxx/tax-invoice/fill/TOKEN" │
│       │                                                         │
│       ▼                                                         │
│  ลูกค้ากดลิงก์ → หน้าฟอร์ม (public, ไม่ต้อง login)                 │
│    - เห็นข้อมูลออเดอร์ (read-only)                                │
│    - กรอกข้อมูลใบกำกับภาษี                                       │
│    - กดส่ง                                                       │
│       │                                                         │
│       ▼                                                         │
│  ระบบบันทึก taxInfo → status เปลี่ยนเป็น "submitted"               │
│  แสดงหน้า "ขอบคุณ" + สรุปข้อมูล                                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                         ADMIN FLOW                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  แอดมินเข้าหน้า /admin/tax-invoices                              │
│       │                                                         │
│       ▼                                                         │
│  เห็นรายการคำขอใบกำกับภาษีทั้งหมด (filter ตาม status ได้)          │
│       │                                                         │
│       ▼                                                         │
│  กดดูรายละเอียด → เห็น taxInfo + ข้อมูลออเดอร์                    │
│       │                                                         │
│       ▼                                                         │
│  เปลี่ยนสถานะ: submitted → processing → completed                │
│  เพิ่ม adminNotes ได้                                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. Data Model

### 3.1 MongoDB Collection: `tax_invoices`

```javascript
{
  _id: ObjectId,

  // === ความสัมพันธ์ (Relations) ===
  orderId: ObjectId,              // FK → orders._id
  userId: String,                 // userId จากแชท เช่น "Uf1a2b3c..." (LINE) หรือ "12345..." (FB)
  platform: String,               // "line" | "facebook"
  botId: ObjectId | null,         // FK → line_bots._id หรือ facebook_bots._id

  // === Token สำหรับลิงก์ฟอร์ม ===
  token: String,                  // unique, crypto random, 32 chars
                                  // ใช้เป็น URL identifier แทน ObjectId เพื่อความปลอดภัย

  // === Snapshot ข้อมูลออเดอร์ ณ ตอนสร้างคำขอ ===
  orderSnapshot: {
    items: [
      {
        product: String,          // ชื่อสินค้า
        quantity: Number,
        price: Number
      }
    ],
    totalAmount: Number,          // ยอดรวม
    shippingCost: Number,         // ค่าส่ง
    customerName: String          // ชื่อลูกค้า
  },

  // === ข้อมูลใบกำกับภาษีที่ลูกค้ากรอก ===
  // เริ่มต้นเป็น null ทั้งหมด จนกว่าลูกค้าจะกรอกฟอร์ม
  taxInfo: {
    type: String,                 // "personal" (บุคคลธรรมดา) | "company" (นิติบุคคล)
    name: String,                 // ชื่อบริษัท หรือ ชื่อ-นามสกุล
    taxId: String,                // เลขประจำตัวผู้เสียภาษี (13 หลัก)
    branchCode: String,           // รหัสสาขา ("00000" = สำนักงานใหญ่)
                                  // แสดงเฉพาะเมื่อ type = "company"
    address: String,              // ที่อยู่
    subDistrict: String,          // แขวง/ตำบล
    district: String,             // เขต/อำเภอ
    province: String,             // จังหวัด
    postalCode: String,           // รหัสไปรษณีย์ (5 หลัก)
    phone: String,                // เบอร์โทร (10 หลัก)
    email: String | null,         // อีเมล (optional)
    notes: String | null          // หมายเหตุจากลูกค้า (optional)
  } | null,                       // null = ลูกค้ายังไม่กรอก

  // === สถานะ ===
  status: String,
  // "pending"    → สร้างแล้ว รอลูกค้ากรอก
  // "submitted"  → ลูกค้ากรอกแล้ว รอแอดมิน
  // "processing" → แอดมินกำลังจัดทำใบกำกับภาษี
  // "completed"  → ออกใบกำกับภาษีเรียบร้อย
  // "cancelled"  → ยกเลิก

  // === Timestamps ===
  createdAt: Date,                // สร้างคำขอ
  submittedAt: Date | null,       // ลูกค้ากรอกเสร็จ
  completedAt: Date | null,       // แอดมินทำเสร็จ
  updatedAt: Date,

  // === Admin ===
  adminNotes: String              // หมายเหตุจากแอดมิน
}
```

### 3.2 Indexes

```javascript
{ token: 1 }           // unique — ค้นหาจาก token ตอนลูกค้ากรอกฟอร์ม
{ orderId: 1 }         // ค้นหาคำขอจาก orderId (ป้องกันซ้ำ)
{ userId: 1 }          // ค้นหาคำขอทั้งหมดของลูกค้า
{ status: 1 }          // filter ใน admin page
{ createdAt: -1 }      // เรียงตามวันที่
```

---

## 4. AI Tool Definition

### 4.1 Tool: `request_tax_invoice`

```javascript
{
  type: "function",
  function: {
    name: "request_tax_invoice",
    description: `สร้างลิงก์ให้ลูกค้ากรอกข้อมูลสำหรับออกใบกำกับภาษี (Tax Invoice)
ใช้เมื่อ: ลูกค้าขอใบกำกับภาษี, tax invoice, ใบกำกับ
ขั้นตอน: 
1. เรียก get_orders ก่อนเพื่อดึง orderId
2. เรียก request_tax_invoice พร้อม orderId
3. ส่งลิงก์ที่ได้ให้ลูกค้า พร้อมบอกให้กรอกข้อมูล
⚠️ ต้องมี orderId เสมอ — ถ้าลูกค้ายังไม่มีออเดอร์ ให้แจ้งว่าต้องสั่งซื้อก่อน`,
    parameters: {
      type: "object",
      properties: {
        orderId: {
          type: "string",
          description: "รหัสออเดอร์ที่ต้องการออกใบกำกับภาษี (ได้จาก get_orders)"
        }
      },
      required: ["orderId"]
    }
  }
}
```

### 4.2 เพิ่มใน ORDER_TOOL_INSTRUCTIONS

```
4. request_tax_invoice - สร้างลิงก์ให้ลูกค้ากรอกข้อมูลใบกำกับภาษี (ต้องมี orderId)

⚡ เมื่อไหร่ต้องใช้ request_tax_invoice:
- ลูกค้าพูดถึง "ใบกำกับภาษี" "tax invoice" "ออกใบกำกับ"
- ต้องเรียก get_orders ก่อนเพื่อได้ orderId
- ส่งลิงก์ที่ได้ให้ลูกค้า พร้อมบอกให้กรอกข้อมูลบริษัท/ผู้เสียภาษี
- ถ้าลูกค้ายังไม่มี order → แจ้งให้สั่งซื้อก่อน
```

### 4.3 Tool Return Value

**สำเร็จ:**
```json
{
  "success": true,
  "formUrl": "https://example.com/tax-invoice/fill/aBcDeFgHiJkL...",
  "message": "สร้างลิงก์กรอกข้อมูลใบกำกับภาษีแล้ว ส่งลิงก์นี้ให้ลูกค้าเพื่อกรอกข้อมูล"
}
```

**มีคำขอซ้ำ (ยังไม่กรอก):**
```json
{
  "success": true,
  "formUrl": "https://example.com/tax-invoice/fill/aBcDeFgHiJkL...",
  "message": "มีคำขอใบกำกับภาษีสำหรับออเดอร์นี้อยู่แล้ว (รอลูกค้ากรอกข้อมูล) — ส่งลิงก์เดิมให้ลูกค้า"
}
```

**มีคำขอที่กรอกแล้ว:**
```json
{
  "success": false,
  "error": "ออเดอร์นี้มีคำขอใบกำกับภาษีที่ส่งข้อมูลแล้ว (สถานะ: submitted) ไม่ต้องสร้างใหม่"
}
```

**Order ไม่พบ:**
```json
{
  "success": false,
  "error": "ไม่พบออเดอร์รหัส xxx หรือไม่ใช่ออเดอร์ของลูกค้ารายนี้"
}
```

---

## 5. API Routes

### 5.1 Customer-Facing (Public)

| Method | Path | Description |
|--------|------|-------------|
| `GET` | `/tax-invoice/fill/:token` | แสดงฟอร์มกรอกข้อมูล (render EJS) |
| `POST` | `/api/tax-invoice/fill/:token` | รับข้อมูลจากฟอร์ม |

### 5.2 Admin (ต้อง login)

| Method | Path | Description |
|--------|------|-------------|
| `GET` | `/admin/tax-invoices` | หน้า admin แสดงรายการคำขอ (render EJS) |
| `GET` | `/api/tax-invoices` | API ดึงรายการคำขอ (JSON, pagination, filter) |
| `GET` | `/api/tax-invoices/:id` | API ดึงรายละเอียดคำขอเดียว |
| `PATCH` | `/api/tax-invoices/:id/status` | เปลี่ยนสถานะ |
| `PATCH` | `/api/tax-invoices/:id/notes` | อัปเดต adminNotes |

---

## 6. หน้า Customer Form (`tax-invoice-form.ejs`)

### 6.1 หน้าจอ

หน้า public ที่ลูกค้าเข้าผ่านลิงก์ — design สะอาด, mobile-first

**ส่วนที่ 1: ข้อมูลออเดอร์ (read-only)**
- แสดง: รายการสินค้า, จำนวน, ราคา, ยอดรวม
- อ้างอิงจาก `orderSnapshot`

**ส่วนที่ 2: ฟอร์มกรอกข้อมูลใบกำกับภาษี**

| ฟิลด์ | Label | Type | Required | Validation | หมายเหตุ |
|-------|-------|------|----------|------------|----------|
| `type` | ประเภท | radio | ✅ | - | "บุคคลธรรมดา" / "นิติบุคคล" |
| `name` | ชื่อ/ชื่อบริษัท | text | ✅ | minLength: 2 | label เปลี่ยนตาม type |
| `taxId` | เลขผู้เสียภาษี | text | ✅ | 13 หลัก, ตัวเลขเท่านั้น | |
| `branchCode` | รหัสสาขา | text | เฉพาะ company | 5 หลัก | "00000" = สำนักงานใหญ่ |
| `address` | ที่อยู่ | textarea | ✅ | minLength: 5 | |
| `subDistrict` | แขวง/ตำบล | text | ✅ | | |
| `district` | เขต/อำเภอ | text | ✅ | | |
| `province` | จังหวัด | text | ✅ | | |
| `postalCode` | รหัสไปรษณีย์ | text | ✅ | 5 หลัก | |
| `phone` | เบอร์โทร | tel | ✅ | 9-10 หลัก | |
| `email` | อีเมล | email | ❌ | email format | |
| `notes` | หมายเหตุ | textarea | ❌ | | |

**ส่วนที่ 3: ปุ่มส่ง**
- ปุ่ม "ส่งข้อมูล"
- กด confirm ก่อนส่ง

**หน้า Thank You (หลังส่งสำเร็จ):**
- "ขอบคุณ! ข้อมูลใบกำกับภาษีของคุณถูกส่งเรียบร้อยแล้ว"
- สรุปข้อมูลที่กรอก
- สถานะ: "รอดำเนินการ"

### 6.2 กรณี Token ไม่ถูกต้อง / หมดอายุ

- Token ไม่พบ → แสดง "ลิงก์ไม่ถูกต้องหรือหมดอายุ"
- Status ไม่ใช่ `pending` → แสดง "ข้อมูลใบกำกับภาษีถูกส่งแล้ว" + สรุปข้อมูลเดิม

### 6.3 UI/UX

- **Mobile-first** — ลูกค้าส่วนใหญ่จะเปิดผ่าน LINE/Facebook บนมือถือ
- **ภาษาไทย** 100%
- กดเลือก type → แสดง/ซ่อน branchCode
- Inline validation (client-side) ก่อน submit
- Loading state ขณะ submit
- ไม่ต้อง login

---

## 7. หน้า Admin (`admin-tax-invoices.ejs`)

### 7.1 รายการคำขอ

- ตารางแสดง: วันที่, ชื่อลูกค้า/บริษัท, ออเดอร์, ยอดเงิน, สถานะ, actions
- **Filter** ตาม status (Tabs: ทั้งหมด / รอกรอก / รอดำเนินการ / กำลังทำ / เสร็จ / ยกเลิก)
- **ค้นหา** ตามชื่อ / เลข Tax ID / orderId
- **Pagination**
- เรียงตาม: วันที่สร้าง (ล่าสุดก่อน)

### 7.2 รายละเอียดคำขอ (Modal/Panel)

- ข้อมูลออเดอร์ (จาก orderSnapshot)
- ข้อมูลใบกำกับภาษี (จาก taxInfo) — ถ้า status เป็น pending จะว่าง
- ปุ่มเปลี่ยนสถานะ
- ช่อง adminNotes

### 7.3 Actions

| Action | สถานะปัจจุบัน | สถานะใหม่ |
|--------|--------------|-----------|
| เริ่มดำเนินการ | submitted | processing |
| ทำเสร็จแล้ว | processing | completed |
| ยกเลิก | pending / submitted | cancelled |

---

## 8. Backend Implementation

### 8.1 Function: `requestTaxInvoiceFromTool(args, context)`

```javascript
async function requestTaxInvoiceFromTool(args = {}, context = {}) {
  // 1. ตรวจสอบ userId
  // 2. ตรวจสอบ orderId format (ObjectId.isValid)
  // 3. ดึง order จาก DB → ตรวจสอบว่า order.userId === context.userId
  // 4. ตรวจสอบซ้ำ: ค้นหาใน tax_invoices ที่มี orderId เดียวกัน
  //    - ถ้ามี + status === "pending" → return ลิงก์เดิม
  //    - ถ้ามี + status !== "pending" → return error (มีคำขอแล้ว)
  // 5. สร้าง token (crypto.randomBytes(24).toString('hex') → 48 chars)
  // 6. สร้าง orderSnapshot จาก order.orderData
  // 7. Insert ลง tax_invoices
  // 8. สร้าง formUrl = PUBLIC_BASE_URL + "/tax-invoice/fill/" + token
  // 9. Return { success: true, formUrl, message }
}
```

### 8.2 Token Generation

```javascript
const crypto = require("crypto");

function generateTaxInvoiceToken() {
  return crypto.randomBytes(24).toString("hex"); // 48 chars hex
}
```

- ใช้ crypto random แทน short link — เพราะ:
  - Token ต้อง unique ต่อ tax invoice (ไม่ใช่ redirect URL)
  - ความยาว 48 chars ทำให้เดาไม่ได้
  - ไม่จำเป็นต้อง short เพราะส่งผ่านแชท (ลูกค้ากดลิงก์ ไม่ต้องพิมพ์)

### 8.3 Ensure Indexes

```javascript
async function ensureTaxInvoiceIndexes(db) {
  const coll = db.collection("tax_invoices");
  await coll.createIndex({ token: 1 }, { unique: true });
  await coll.createIndex({ orderId: 1 });
  await coll.createIndex({ userId: 1 });
  await coll.createIndex({ status: 1, createdAt: -1 });
}
```

เรียกใน `connectDB()` เหมือน indexes อื่น ๆ ของระบบ

---

## 9. Validation Rules

### 9.1 Client-side (ในฟอร์ม)

| ฟิลด์ | Validation |
|-------|-----------|
| `type` | ต้องเลือก "personal" หรือ "company" |
| `name` | ต้องกรอก, >= 2 ตัวอักษร |
| `taxId` | ต้องกรอก, ตัวเลข 13 หลัก |
| `branchCode` | ถ้า type = "company": ต้องกรอก, ตัวเลข 5 หลัก |
| `address` | ต้องกรอก, >= 5 ตัวอักษร |
| `subDistrict` | ต้องกรอก |
| `district` | ต้องกรอก |
| `province` | ต้องกรอก |
| `postalCode` | ต้องกรอก, ตัวเลข 5 หลัก |
| `phone` | ต้องกรอก, ตัวเลข 9-10 หลัก |
| `email` | ถ้ากรอก: ตรวจ format email |
| `notes` | ไม่บังคับ, max 500 ตัวอักษร |

### 9.2 Server-side (POST /api/tax-invoice/fill/:token)

- ตรวจ token → ต้องพบ record + status === "pending"
- ตรวจข้อมูลทุกฟิลด์เหมือน client-side
- Sanitize input (trim, strip HTML)
- ป้องกัน double-submit (เช็ค status ก่อน update)

---

## 10. Security

| ประเด็น | มาตรการ |
|---------|---------|
| Token เดาไม่ได้ | ใช้ `crypto.randomBytes(24)` → 48 hex chars |
| ฟอร์มถูกส่งซ้ำ | เช็ค status ก่อน update, client-side disable ปุ่มหลังส่ง |
| XSS | Sanitize ข้อมูลก่อนบันทึก, EJS escape output |
| ออเดอร์ไม่ใช่ของลูกค้า | ตรวจ `order.userId === context.userId` ใน tool |
| Rate limiting | ใช้ rate limit ที่มีอยู่แล้วของระบบ |
| Token ศคนอื่นกรอก | ยอมรับ — เพราะต้องมีลิงก์ก่อน และข้อมูลไม่ sensitive มาก |

---

## 11. ไฟล์ที่ต้องเพิ่ม/แก้ไข

### 11.1 ไฟล์ใหม่

| ไฟล์ | คำอธิบาย |
|------|----------|
| `views/tax-invoice-form.ejs` | หน้าฟอร์มกรอกข้อมูลสำหรับลูกค้า (public) |
| `views/admin-tax-invoices.ejs` | หน้า admin จัดการคำขอใบกำกับภาษี |
| `docs/tax-invoice-system.md` | เอกสารนี้ |

### 11.2 ไฟล์ที่ต้องแก้ไข

| ไฟล์ | การเปลี่ยนแปลง |
|------|---------------|
| `index.js` | — เพิ่ม `ensureTaxInvoiceIndexes()` ใน `connectDB()` |
| | — เพิ่ม `requestTaxInvoiceFromTool()` function |
| | — เพิ่ม tool definition `request_tax_invoice` ในทั้ง 2 ที่ (text + multimodal) |
| | — เพิ่ม tool handler ใน tool loop ทั้ง 2 ที่ |
| | — เพิ่มใน `ORDER_TOOL_INSTRUCTIONS` |
| | — เพิ่ม routes: GET/POST form, admin page, admin APIs |
| `views/partials/admin-navbar.ejs` | เพิ่มเมนู "ใบกำกับภาษี" |

---

## 12. ตัวอย่างการใช้งาน

### 12.1 ลูกค้าขอใบกำกับภาษี (Happy Path)

```
ลูกค้า: ขอใบกำกับภาษีครับ
AI: (เรียก get_orders) → ได้ orderId: "65a1b2c3..."
    (เรียก request_tax_invoice({ orderId: "65a1b2c3..." }))
    → ได้ formUrl
AI: ได้เลยครับ! กรุณากรอกข้อมูลสำหรับออกใบกำกับภาษีที่ลิงก์นี้เลยครับ:
    https://xxx/tax-invoice/fill/abc123...
    กรอกเสร็จแล้วทางเราจะดำเนินการให้ครับ 😊
```

### 12.2 ลูกค้าขอซ้ำ

```
ลูกค้า: ส่งลิงก์ใบกำกับภาษีให้อีกทีได้มั้ยครับ
AI: (เรียก get_orders → request_tax_invoice)
    → ได้ formUrl เดิม (เพราะ orderId ซ้ำ + status ยัง pending)
AI: ได้เลยครับ ลิงก์เดิมนี้ครับ:
    https://xxx/tax-invoice/fill/abc123...
```

### 12.3 ลูกค้ายังไม่มีออเดอร์

```
ลูกค้า: ขอใบกำกับภาษีครับ
AI: (เรียก get_orders) → ไม่มีออเดอร์
AI: ขออภัยครับ ยังไม่พบออเดอร์ของคุณในระบบ
    กรุณาสั่งซื้อสินค้าก่อนนะครับ แล้วค่อยขอใบกำกับภาษีได้ครับ 😊
```

---

## 13. อนาคต (Out of Scope — พิจารณาภายหลัง)

- **ออก PDF ใบกำกับภาษีอัตโนมัติ** — ใช้ template + puppeteer/pdf-lib
- **แจ้งเตือนแอดมินผ่าน LINE** เมื่อลูกค้ากรอกเสร็จ
- **ส่งใบกำกับภาษีกลับผ่านแชท** — อัปโหลด PDF แล้ว AI ส่งไฟล์ให้
- **หมดอายุอัตโนมัติ** — ถ้าลูกค้าไม่กรอกภายใน X วัน → auto cancel
- **แก้ไขข้อมูลที่กรอกแล้ว** — ลูกค้าแก้ไขได้ก่อนที่แอดมินเริ่มทำ
